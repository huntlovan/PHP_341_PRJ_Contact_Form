# Copy this file to .env and fill in your SMTP + mail settings.
# NEVER commit the real .env file containing secrets.

# SMTP server settings
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_ENCRYPTION=tls          # tls, ssl, or leave blank for none (not recommended)

# Credentials (use an App Password if using Gmail with 2FA)
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password

# Default From identity
MAIL_FROM_ADDRESS=noreply@yourdomain.com
MAIL_FROM_NAME="Your Company Contact"

# Optional overrides for future extensions
# REPLY_TO_ADDRESS=support@yourdomain.com
# REPLY_TO_NAME="Support Desk"
